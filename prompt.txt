关于主界面（Chat 视图）的 UI 体验，目前的布局结构没问题，但在视觉聚焦和交互细节上不够现代。请帮我按照一流 AI 产品的标准（如 Shadcn UI 规范）对以下几个部分进行优化：

1. 优化对话区 (Chat Messages) 阅读体验
- 限制对话内容宽度：目前 AI 的气泡在宽屏下横向拉伸过长。请在 `ChatMessage` 及其外层容器中加入最大宽度限制（例如使用 `max-w-3xl` 或 `max-w-4xl`），并使用 `mx-auto` 让对话流在主区域中居中。
- 修复内联代码/路径的高亮样式：目前 Markdown 渲染出的路径（如 C:\Users\...）是红色字体，容易引起误解（像报错）。请在 Markdown 渲染组件中，将 `code` 标签（非代码块的 inline code）样式统一改为：`bg-muted text-foreground px-1.5 py-0.5 rounded-md text-sm font-mono border border-border/50`。

2. 重构底部输入区 (Input Area)
- 迁移模型指示器：目前右下角的 `deepseek-chat` 标签嵌在输入框内部，容易遮挡长文本。请将其移出输入框容器，放到输入框的上方左侧，或者放到输入框正下方的免责声明文字旁边。
- 增强输入框质感：给输入框外层容器加上更明显的阴影和聚焦态交互：`shadow-sm border border-input focus-within:ring-1 focus-within:ring-primary focus-within:border-primary transition-all bg-background`。

3. 整理顶部 Header
- 右侧的“文件”和“Agent”按钮目前使用了灰底，显得有些厚重。请把它们统一替换为 Shadcn Button 的 `variant="ghost"` 或 `variant="outline"`，让顶部更通透。
- 建议将其配套的图标换成 `lucide-react` 中类似 `PanelRight` 这种明确表达“打开右侧面板”语义的图标。

4. 优化左侧侧边栏 (Sidebar) 细节
- 强化选中状态：目前左侧选中的会话（如 KTClaw）视觉对比度不够。请修改选中态的样式，例如加深背景色（`bg-accent text-accent-foreground`），或者在左侧加上一条主题色边框（`border-l-4 border-primary`），让当前上下文一目了然。
- 统一图标：排查侧边栏的图标（任务看板、团队总览等），确保全部使用 `lucide-react`，并且统一 `strokeWidth`（建议设为 1.5 ），保持线条粗细一致。

请按上述要求排查对应的文件（如 Chat/index.tsx, ChatMessage.tsx, 侧边栏和 Header 组件等），并直接修改 Tailwind 类名和组件结构。