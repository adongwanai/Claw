@Claude，目前的 UI 视觉非常克制和清爽，这正是我想要的。但是这次重构破坏了核心的业务逻辑，请立刻修复以下两个严重级别的 Bug：

1. 恢复中间栏 (Session List) 右上角的 + 按钮

问题：之前由于误解，中间栏右上角的 + 按钮被移除了。但用户在选中某个 Bot（如 Feishu）后，需要通过这个按钮来**“同步新群聊”或“发起新会话”**。

修复：请将 + 按钮加回中间栏的 Header（在搜索框上方或同级）。点击该按钮时，应该触发当前选中渠道对应的“新增会话/同步群聊”的逻辑或弹窗，而不是去选渠道类型。

2. 彻底修复消息发送链路 (Critical Bug)

问题：目前在底部输入框输入文本后，点击发送按钮（或按 Enter）毫无反应。我的消息没有上屏（未渲染在消息列表中），大模型也没有进行回复。

修复要求：请仔细检查 MessageComposer 组件的发送逻辑，确保完整的发送生命周期恢复正常：

事件绑定：确保右下角的发送按钮和输入框的 onKeyDown (Enter 键) 正确绑定了 handleSend（或类似命名）的方法。

乐观更新 (Optimistic UI)：点击发送后，用户输入的信息必须立刻作为一个气泡（isSelf: true，根据右下角的身份 toggle 决定）推入当前的消息列表中，不要干等后端响应。

API 调用与状态流转：确保调用了正确的 sendMessage 接口（Channel Store 或对应的 Context 里的发送方法），将消息 payload 正确推给后端 Gateway/Agent。

清空输入框：发送动作触发后，必须清空当前的文本输入框。

请检查并修复以上问题。如果你发现 Phase 10/11 的原有发送逻辑被不小心删除了，请从版本历史中找回并正确挂载。修复完成后，请简要告诉我你找回了哪段发送逻辑。